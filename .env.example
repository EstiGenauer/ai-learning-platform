# OpenAI API key (required for AI lesson generation)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Model name — use gpt-4o if gpt-4o-mini is not enabled on your OpenAI project
OPENAI_MODEL=gpt-4o

# Used by docker-compose for backend
# JWT key is configured in backend appsettings.json
