apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: learning-platform
  labels:
    app: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: learning-platform-auth-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__DefaultConnection
              value: "Host=auth-db;Port=5432;Database=auth_db;Username=admin;Password=password;Timeout=5;Command Timeout=30"
            - name: Services__AiService
              value: "http://ai-service:8080"
            - name: Jwt__Key
              valueFrom:
                secretKeyRef:
                  name: learning-platform-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: learning-platform
spec:
  selector:
    app: auth-service
  ports:
    - port: 8080
      targetPort: 8080
